# efficient-masked-diffusion

Sequel to `masked-diffusion`. Covers the systems side of non-autoregressive
generation: KV caching, causal attention, why masked diffusion breaks the
caching assumption, and block diffusion.

Assumes the reader has read the intro blog first.
